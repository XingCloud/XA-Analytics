- stylesheet "menus"
.row-fluid
  .span2
    = render :partial => "left_nav", :locals => {:project => @project, :menus => @menus}

  .span10
    .hero
      还没有为该菜单设置报告，
      = link_to "点击这里进行配置", edit_project_menu_path(@project, @menu)
