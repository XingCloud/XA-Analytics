- javascript "menus"

- title "菜单管理"

.page-header
  %h3
  可拖动菜单，目前只支持两级之间的拖拽。
.row-fluid
  .span3
  .span9
    .well.sidebar-nav
      #sortable
        = nested_list @menus.roots do |menu|
          - if menu.child?
            %div= link_to menu.name, edit_project_menu_path(@project,menu)
          -else
            %div= "#{menu.name}"
        .form-actions
        = form_tag reorder_project_menus_path(@project),:class=>'form-horizontal' do
          = submit_tag '保存',:class => 'btn btn-primary'
          = link_to "取消", project_menus_path(@project), :class => "btn"