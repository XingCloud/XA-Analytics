.row
  .span3
    .well.sidebar-nav
      = nested_list @menus.roots do |menu|
        %div= "#{menu.name}"
        = link_to "编辑",edit_menu_path(menu)
        = link_to "删除",menu,:confirm=>'Are you sure?',:method => :delete

%span= link_to "返回", project_path(@project)