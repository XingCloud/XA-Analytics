= simple_form_for @menu, :as => :menu, :url => @menu.new_record? ? admin_template_menus_path : admin_template_menu_path(@menu),:html => {:class => "form-horizontal"} do |f|
  .page-header
    .h3
      一般信息
      %small 菜单名称及描述
  .form-stocked

    %fieldset
      .row
        .span6
          = f.input :name
          .control-group.string.optional
            %label.string.optional.control-label
              %div　报表
              %div.controls
                = select_tag :report_id, options_for_select(@reports.map {|p| [ p.title, p.id ] },@menu.report_ids),{ :multiple => true, :size =>4, :id => "menu_report" }
          = f.input :desc

  .form-actions
    = f.submit "提交", :class => "btn btn-primary"
    = link_to "取消", admin_template_menus_path, :class => "btn"