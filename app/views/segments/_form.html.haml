- stylesheet "segments"

.span2
  - if not @project.nil?
    = render "projects/left_nav"
  - else
    = render "admin/projects/left_nav"

.span10

  - if @segment.persisted?
    %h3 编辑用户群
    %hr
  - else
    %h3 新建用户群
    %hr
  = render "segments/common"